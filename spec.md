# 远程桌面控制系统 - 产品需求规格

## 项目概述

基于 WebRTC 的远程桌面控制系统，允许 Android 设备在局域网内操作 Windows 电脑桌面。

**技术栈**：Android (Flutter) → Windows (Python) → 极简信令服务器 (Node.js + WebSocket)

---

## 产品需求

### 核心功能
1. **屏幕投射**：将 Windows 桌面实时投射到 Android 设备
2. **鼠标控制**：通过触摸屏幕控制远程鼠标（移动、点击）
3. **键盘输入**：支持文本输入到远程电脑
4. **横屏操作**：强制横屏显示，提供更好的桌面操作体验

### 用户体验需求
1. **自动连接**：启动后自动连接信令服务器和远程主机
2. **状态可见**：实时显示连接状态（连接中/已连接/断开）
3. **分辨率适配**：根据远程屏幕分辨率自动调整显示比例
4. **深色主题**：暗黑玻璃态科技风格 UI，保护眼睛
5. **服务器配置**：支持用户自定义信令服务器地址（IP + 端口）
6. **控制面板**：滑出式面板显示连接信息和操作说明

### 连接流程
1. 启动信令服务器（signal）
2. 启动受控端（host），注册为 host 角色
3. 启动控制端（client），注册为 client 角色
4. WebRTC P2P 连接建立
5. 视频流开始传输，控制端可操作

### 控制指令
| 动作 | 说明 |
|------|------|
| 点击 | 鼠标左键点击 |
| 移动 | 鼠标移动到指定坐标 |
| 输入 | 键盘文本输入 |

---

## 环境要求

### Host (Windows)
- Python 3.x 环境
- 用户会话中运行（不支持系统服务账号）
- 管理员权限（用于 pyautogui 控制）
- 防火墙放行 8080 端口

### Client (Android)
- Flutter 环境
- Android 设备（开发模式）
- 局域网连接

### Signal Server
- Node.js 环境
- 局域网内可访问
- 端口 8080（可配置）

---

## 网络要求
- 三端必须在同一局域网内
- 使用 STUN 服务器辅助连接
- 暂不涉及 NAT 穿透（无 TURN 服务器）

---

## 已知限制
1. 仅支持局域网环境，无公网穿透能力
2. 手势仅支持单击，暂不支持长按、滑动、双击
3. 键盘输入需在客户端界面实现，当前仅 host 端预留接口
4. 无身份验证机制，同一局域网内任何客户端可连接
5. 无加密传输，视频流和控制指令均为明文

---

## 更新日志

| 日期 | 版本 | 说明 |
|------|------|------|
| 2025-04-28 | v0.1.0 | 初始版本，完成基础投屏和鼠标控制 |
| 2025-05-07 | v0.2.0 | 完善坐标映射算法，横屏展示，优化客户端 UI |
| 2025-05-07 | v0.2.1 | 暗黑玻璃态科技风格 UI，添加控制面板和状态指示 |
| 2025-05-07 | v0.2.2 | 添加服务器地址动态配置功能（IP + 端口可设置） |
